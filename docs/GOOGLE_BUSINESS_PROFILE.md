# Google Business Profile

When the Jane.M Google Business Profile has been verified, add its full public URL to `JaneM_Website/seo.config.json` as the value of `googleBusinessProfileUrl`, then run `npm run build`.

The build will include that verified profile in the business structured data. Leave the value blank until the public profile URL is confirmed; do not use a search URL or an unverified listing.
